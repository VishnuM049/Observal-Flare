services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    command: uvicorn server.main:app --host 0.0.0.0 --port 8000 --workers 2
    volumes:
      - /etc/flare:/etc/flare:ro
    restart: unless-stopped

  worker:
    build:
      context: .
      dockerfile: Dockerfile.api
    network_mode: "host"
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/flare
      REDIS_URL: redis://localhost:6379
      GOOGLE_APPLICATION_CREDENTIALS: /etc/flare/gcp-credentials.json
      CLOUDSDK_AUTH_CREDENTIAL_FILE_OVERRIDE: /etc/flare/gcp-credentials.json
    volumes:
      - /etc/flare:/etc/flare:ro
      - worker-ssh:/root/.ssh
    restart: unless-stopped

  web:
    build:
      context: ./web
      dockerfile: Dockerfile.prod
    command: npm start
    restart: unless-stopped

  db:
    restart: unless-stopped

  redis:
    restart: unless-stopped

volumes:
  worker-ssh:
